# kev-dual — Roadmap

One model that thinks and decides: text generation plus Kev-style typed, calibrated decisions from one set of loaded weights.

<!-- generated by cairn; edit the files in `cairn/items` and run `cairn render` -->

## load — Load: one model, two modes

`##########` 100% · 6 of 6 done · due 2026-09-25

Kev-4B's decide mode and Qwen3.5-4B's text mode run from one loaded set of weights on Apple Silicon (MLX).

### done

- [x] `0008` Adopt cairn and write the research and context docs <sup>chore · p1 · infra</sup>
- [x] `0009` Set up the package: dependencies, lint and test markers <sup>chore · p0 · infra</sup>
- [x] `0010` Can Kev's PEFT adapter run unmerged in mlx-lm with a runtime on/off? <sup>spike · p0 · model</sup>
- [x] `0011` DualModel on MLX: one set of weights, decide and text modes <sup>feature · p0 · model</sup>
- [x] `0012` Baseline cost of each mode and of a mode switch on Kev-4B <sup>experiment · p1 · model · pass</sup>
- [x] `0013` Torch/CUDA backend of DualModel for Modal <sup>feature · p2 · serving</sup>

## write — Write: choose how text mode writes

`###·······` 22% · 2 of 9 done · due 2026-09-30

Kev is trained on Base checkpoints, which write poorly. Measure how poorly, on the text an agent step actually needs, before choosing a fix. See docs/design/architecture.md.

### backlog

- [ ] `0018` Second LoRA for generation on the Base checkpoint (fallback) <sup>experiment · p3 · training</sup>
- [ ] `0040` Run the torch weight tests on a Modal CUDA GPU <sup>chore · p1 · infra</sup>

### planned

- [ ] `0014` What text does an agent step actually need? <sup>spike · p0 · text-eval</sup>
- [ ] `0015` Text-mode eval harness <sup>feature · p0 · text-eval</sup>
- [ ] `0016` Text mode on Base vs Instruct <sup>experiment · p0 · text-eval</sup>
- [ ] `0017` Kev-4B retrained on Qwen3.5-4B Instruct <sup>experiment · p1 · training</sup>
- [ ] `0039` What state format serves both modes? <sup>spike · p1 · text-eval</sup>

### done

- [x] `0041` Enforce the workflow: CI, required checks and squash-only merges <sup>chore · p0 · infra</sup>
- [x] `0042` Fix the two edge cases from the code review <sup>bug · p2 · model</sup>

## share — Share: one prefill, two modes

`##········` 20% · 1 of 5 done · due 2026-10-09

Whole-model LoRA changes the state's hidden states, so switching modes means prefilling the page twice. A question-side adapter (off over the state, on from `<q>` onward) makes the state cache shared by both modes.

### backlog

- [ ] `0020` Question-side adapter placement <sup>feature · p0 · training</sup>
- [ ] `0021` Question-side Kev-4B <sup>experiment · p0 · training</sup>
- [ ] `0022` One prefill serves both modes <sup>feature · p0 · model</sup>
- [ ] `0023` Per-step latency: shared prefill vs two prefills vs two models <sup>experiment · p1 · model</sup>

### done

- [x] `0019` What is still new after Solomon and Kev's question-side LoRA plan? <sup>spike · p0 · research</sup>

## act — Act: agent loop on Mind2Web

`··········` 0% · 0 of 6 done · due 2026-10-16

The demo that carries the launch: a web-agent step where one model picks the element and operation as typed decisions and generates the value to type.

### backlog

- [ ] `0024` Freeze the Mind2Web eval split and step format <sup>feature · p0 · data</sup>
- [ ] `0025` Agent step: element and operation as decisions, the value as text <sup>feature · p0 · agent</sup>
- [ ] `0026` Mind2Web scorer <sup>feature · p0 · agent</sup>
- [ ] `0027` Agent baselines: Kev plus a separate LLM, and the LLM alone <sup>feature · p1 · agent</sup>
- [ ] `0028` Zero-shot kev-dual on Mind2Web <sup>experiment · p0 · agent</sup>
- [ ] `0029` Fine-tune decide mode on Mind2Web train <sup>experiment · p1 · training</sup>

## bench — Bench: decision quality holds

`··········` 0% · 0 of 2 done · due 2026-10-19

Show that the changes needed for two modes (Instruct base, question-side placement) did not cost decision quality.

### backlog

- [ ] `0030` How does a jevbench submission work? <sup>spike · p1 · decide-eval</sup>
- [ ] `0031` Decision quality of the shipped checkpoint <sup>experiment · p1 · decide-eval</sup>

## launch — Launch

`··········` 0% · 0 of 5 done · due 2026-10-23

Open weights, a one-line install, a 30-second side-by-side video, one repeatable number, and an honest comparison table (docs/context/project.md, Launch playbook).

### backlog

- [ ] `0032` Serve both modes from one process <sup>feature · p0 · serving</sup>
- [ ] `0033` README, model card and the honest comparison table <sup>docs · p0 · launch</sup>
- [ ] `0034` Publish weights to the Hub <sup>chore · p0 · launch</sup>
- [ ] `0035` 30-second side-by-side video <sup>docs · p1 · launch</sup>
- [ ] `0036` Re-run the prior-art scan the day before launch <sup>spike · p0 · research</sup>

## later — Later

`··········` 0% · 0 of 2 done

Ideas worth keeping that are not on the path to launch.

### backlog

- [ ] `0037` Pixels: screenshots through the vision tower <sup>feature · p3 · model</sup>
- [ ] `0038` kev-dual at 0.8B and 9B <sup>experiment · p3 · training</sup>

